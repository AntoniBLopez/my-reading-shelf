# Optional: leave empty to use the app in local-only mode (data stored in this browser).
# Fill in to enable cloud sync with Supabase.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Base path: set to "/" for Vercel (or root deployment). Leave unset for GitHub Pages (/my-reading-shelf/).
# VITE_BASE_PATH=/
