# Optional: leave empty to use the app in local-only mode (data stored in this browser).
# Fill in to enable cloud sync with Supabase.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# URL de la app en producción. Usada en enlaces de email (confirmación, recuperar contraseña).
# En Supabase Dashboard → Authentication → URL Configuration pon la misma URL en "Site URL" y en "Redirect URLs".
# Ejemplo: https://my-reading-shelf.vercel.app
# VITE_APP_URL=

# Base path: set to "/" for Vercel (or root deployment). Leave unset for GitHub Pages (/my-reading-shelf/).
# VITE_BASE_PATH=/
